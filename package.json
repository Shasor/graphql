{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.10",
    "tailwindcss": "^4.1.10"
  },
  "scripts": {
    "build": "rsync -av --exclude 'dist' ./ dist/ && tailwindcss -i ./static/css/input.css -o ./dist/static/css/output.css --minify",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
